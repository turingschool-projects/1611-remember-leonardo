<h1>REMEMBER LEONARDO</h1>

{{#each model as |reminder|}}

  {{#link-to 'reminders.reminder' reminder}}
    <div class='reminder-item'>
      <h3>{{reminder.title}}</h3>
    </div>
  {{/link-to}}

{{/each}}

{{outlet}}

